# BrowserLeaks.io 多语言架构文档

**版本**: v1.0
**更新日期**: 2025-11-15

---

## 🌍 多语言策略

### 核心目标

建立一个 **URL 极简**、SEO 完美、代码复用率 100% 的多语言系统。

**路由规则**:
- ✅ **英文 (默认)**: `browserleaks.io/about` (无 `/en` 前缀)
- ✅ **中文**: `browserleaks.io/zh/about`
- ✅ **日文**: `browserleaks.io/ja/about`
- ✅ **法文**: `browserleaks.io/fr/about`
- ✅ **德文**: `browserleaks.io/de/about`

### 设计原则

1. **默认语言无前缀** - 英文是主要市场，URL最简洁
2. **SEO友好** - 每个语言版本独立URL，利于搜索引擎索引
3. **代码零重复** - 所有页面只写一份，通过翻译文件切换内容
4. **自动语言检测** - 首次访问根据浏览器语言自动重定向
5. **语言切换保持状态** - 切换语言时保持当前页面路径

---

## 🔧 技术栈

| 技术 | 版本 | 用途 |
|-----|------|------|
| **next-intl** | ^3.0.0 | Next.js 国际化核心库 |
| **Next.js** | 15.x | App Router 支持 |
| **React** | 19.x | UI渲染 |

**安装**:
```bash
npm install next-intl
```

---

## 📁 文件结构

```
apps/web/
├── app/
│   ├── [locale]/                 # 🚨 所有页面都在这里！
│   │   ├── layout.tsx            # 根布局（设置 html lang）
│   │   ├── page.tsx              # 首页
│   │   ├── tests/
│   │   │   ├── ip-leak/page.tsx
│   │   │   ├── dns-leak/page.tsx
│   │   │   └── ...
│   │   ├── docs/
│   │   │   └── page.tsx
│   │   └── blog/
│   │       ├── page.tsx
│   │       └── [slug]/page.tsx
│   └── not-found.tsx             # 根级 404
│
├── locales/                      # 翻译文件
│   ├── en.json                   # 英文（默认）
│   ├── zh.json                   # 简体中文
│   ├── zh-TW.json                # 繁体中文
│   ├── ja.json                   # 日文
│   ├── ko.json                   # 韩文
│   ├── fr.json                   # 法文
│   ├── de.json                   # 德文
│   ├── es.json                   # 西班牙文
│   ├── pt.json                   # 葡萄牙文
│   ├── ru.json                   # 俄文
│   ├── ar.json                   # 阿拉伯文
│   └── ...                       # 目标：20+语言
│
├── middleware.ts                 # 🚨 核心：URL路由控制
├── i18n.ts                       # next-intl 配置
├── next.config.js                # Next.js 配置
└── package.json
```

**重要规则**:
- ❌ **禁止** 在 `app/` 根目录直接创建 `page.tsx`（除了 `[locale]` 文件夹）
- ✅ **所有页面** 都必须在 `app/[locale]/` 内部
- ✅ **翻译文件** 统一放在 `locales/` 目录

---

## ⚙️ 核心配置

### 1. `middleware.ts` (项目根目录)

```typescript
import createMiddleware from 'next-intl/middleware';

export default createMiddleware({
  // 支持的语言列表
  locales: [
    'en',    // 英文（默认）
    'zh',    // 简体中文
    'zh-TW', // 繁体中文
    'ja',    // 日文
    'ko',    // 韩文
    'fr',    // 法文
    'de',    // 德文
    'es',    // 西班牙文
    'pt',    // 葡萄牙文
    'ru',    // 俄文
    'ar',    // 阿拉伯文
    'hi',    // 印地文
    'tr',    // 土耳其文
    'vi',    // 越南文
    'th',    // 泰文
    'id',    // 印尼文
    'it',    // 意大利文
    'nl',    // 荷兰文
    'pl',    // 波兰文
    'sv',    // 瑞典文
  ],

  // 默认语言
  defaultLocale: 'en',

  // 🚨 关键配置：'as-needed'
  // 效果：默认语言 (en) 不显示前缀，其他语言显示
  localePrefix: 'as-needed',

  // 自动检测浏览器语言
  localeDetection: true,
});

export const config = {
  // 匹配所有非静态资源路径
  matcher: ['/((?!api|_next|_vercel|.*\\..*).*)']
};
```

**关键参数说明**:

| 参数 | 值 | 作用 |
|-----|---|------|
| `locales` | 数组 | 支持的语言列表 |
| `defaultLocale` | `'en'` | 默认语言，URL无前缀 |
| `localePrefix` | `'as-needed'` | 默认语言无前缀，其他语言有前缀 |
| `localeDetection` | `true` | 自动检测浏览器语言 |

---

### 2. `i18n.ts` (项目根目录)

```typescript
import { notFound } from 'next/navigation';
import { getRequestConfig } from 'next-intl/server';

// 支持的语言列表（与 middleware.ts 保持一致）
const locales = [
  'en', 'zh', 'zh-TW', 'ja', 'ko', 'fr', 'de', 'es', 'pt', 'ru',
  'ar', 'hi', 'tr', 'vi', 'th', 'id', 'it', 'nl', 'pl', 'sv'
];

export default getRequestConfig(async ({ locale }) => {
  // 验证语言是否支持
  if (!locales.includes(locale as any)) {
    notFound();
  }

  // 动态加载对应的翻译文件
  return {
    messages: (await import(`./locales/${locale}.json`)).default
  };
});
```

---

### 3. `next.config.js`

```javascript
const withNextIntl = require('next-intl/plugin')();

/** @type {import('next').NextConfig} */
const nextConfig = {
  // 其他配置...
};

module.exports = withNextIntl(nextConfig);
```

---

### 4. `app/[locale]/layout.tsx` (根布局)

```typescript
import { NextIntlClientProvider } from 'next-intl';
import { getMessages } from 'next-intl/server';
import { notFound } from 'next/navigation';

// 支持的语言列表
const locales = ['en', 'zh', 'zh-TW', 'ja', 'ko', 'fr', 'de', 'es', 'pt', 'ru'];

export function generateStaticParams() {
  return locales.map((locale) => ({ locale }));
}

export default async function LocaleLayout({
  children,
  params: { locale }
}: {
  children: React.ReactNode;
  params: { locale: string };
}) {
  // 验证语言
  if (!locales.includes(locale as any)) {
    notFound();
  }

  // 获取翻译消息
  const messages = await getMessages();

  return (
    <html lang={locale}>
      <head>
        {/* SEO: 多语言元标签 */}
        <link rel="alternate" hrefLang="en" href="https://browserleaks.io" />
        <link rel="alternate" hrefLang="zh" href="https://browserleaks.io/zh" />
        <link rel="alternate" hrefLang="ja" href="https://browserleaks.io/ja" />
        {/* ... 其他语言 */}
      </head>
      <body>
        <NextIntlClientProvider messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

---

## 📝 翻译文件规范

### 文件结构 (`locales/en.json`)

```json
{
  "common": {
    "nav": {
      "home": "Home",
      "tests": "Tests",
      "docs": "Documentation",
      "api": "API",
      "blog": "Blog"
    },
    "footer": {
      "copyright": "© 2025 BrowserLeaks.io. All rights reserved.",
      "privacy": "Privacy Policy",
      "terms": "Terms of Service"
    },
    "actions": {
      "scan": "Start Scan",
      "share": "Share",
      "export": "Export",
      "copy": "Copy",
      "close": "Close"
    }
  },

  "home": {
    "hero": {
      "title": "Comprehensive Browser Privacy Testing",
      "subtitle": "Test your browser for IP leaks, DNS leaks, WebRTC leaks, and fingerprinting",
      "cta": "Start Free Scan"
    },
    "privacyScore": {
      "title": "Your Privacy Score",
      "description": "Based on 30+ detection tests",
      "riskLevels": {
        "low": "Low Risk",
        "medium": "Medium Risk",
        "high": "High Risk",
        "critical": "Critical Risk"
      }
    }
  },

  "tests": {
    "ipLeak": {
      "title": "IP Leak Test",
      "description": "Check if your real IP address is exposed",
      "results": {
        "ipv4": "IPv4 Address",
        "ipv6": "IPv6 Address",
        "country": "Country",
        "city": "City",
        "isp": "ISP",
        "isVPN": "VPN Detected",
        "isProxy": "Proxy Detected"
      }
    },
    "dnsLeak": {
      "title": "DNS Leak Test",
      "description": "Detect DNS queries leaking your location",
      "results": {
        "servers": "DNS Servers",
        "isLeak": "Leak Detected",
        "leakType": "Leak Type",
        "recommendations": "Recommendations"
      }
    },
    "webrtcLeak": {
      "title": "WebRTC Leak Test",
      "description": "Check for IP leaks via WebRTC",
      "results": {
        "localIPs": "Local IPs",
        "publicIPs": "Public IPs",
        "natType": "NAT Type",
        "mdnsLeak": "mDNS Leak"
      }
    }
    // ... 其他检测项
  },

  "reports": {
    "title": "Privacy Report",
    "generatedAt": "Generated at",
    "summary": "Summary",
    "vulnerabilities": "Vulnerabilities",
    "recommendations": "Recommendations"
  }
}
```

### 中文翻译 (`locales/zh.json`)

```json
{
  "common": {
    "nav": {
      "home": "首页",
      "tests": "检测",
      "docs": "文档",
      "api": "API",
      "blog": "博客"
    },
    "footer": {
      "copyright": "© 2025 BrowserLeaks.io. 保留所有权利。",
      "privacy": "隐私政策",
      "terms": "服务条款"
    },
    "actions": {
      "scan": "开始扫描",
      "share": "分享",
      "export": "导出",
      "copy": "复制",
      "close": "关闭"
    }
  },

  "home": {
    "hero": {
      "title": "全面的浏览器隐私检测",
      "subtitle": "检测您的浏览器是否存在IP泄露、DNS泄露、WebRTC泄露和浏览器指纹",
      "cta": "开始免费扫描"
    },
    "privacyScore": {
      "title": "您的隐私评分",
      "description": "基于30+种检测测试",
      "riskLevels": {
        "low": "低风险",
        "medium": "中等风险",
        "high": "高风险",
        "critical": "严重风险"
      }
    }
  },

  "tests": {
    "ipLeak": {
      "title": "IP泄露检测",
      "description": "检查您的真实IP地址是否泄露",
      "results": {
        "ipv4": "IPv4地址",
        "ipv6": "IPv6地址",
        "country": "国家",
        "city": "城市",
        "isp": "运营商",
        "isVPN": "检测到VPN",
        "isProxy": "检测到代理"
      }
    },
    "dnsLeak": {
      "title": "DNS泄露检测",
      "description": "检测DNS查询是否泄露您的位置",
      "results": {
        "servers": "DNS服务器",
        "isLeak": "检测到泄露",
        "leakType": "泄露类型",
        "recommendations": "建议"
      }
    },
    "webrtcLeak": {
      "title": "WebRTC泄露检测",
      "description": "检查WebRTC是否泄露IP",
      "results": {
        "localIPs": "本地IP",
        "publicIPs": "公网IP",
        "natType": "NAT类型",
        "mdnsLeak": "mDNS泄露"
      }
    }
    // ... 其他检测项
  },

  "reports": {
    "title": "隐私报告",
    "generatedAt": "生成时间",
    "summary": "摘要",
    "vulnerabilities": "漏洞",
    "recommendations": "建议"
  }
}
```

---

## 💻 组件开发规范

### 1. 服务端组件 (推荐)

```typescript
// app/[locale]/page.tsx
import { useTranslations } from 'next-intl';

export default function HomePage() {
  const t = useTranslations('home');

  return (
    <main>
      <h1>{t('hero.title')}</h1>
      <p>{t('hero.subtitle')}</p>
      <button>{t('hero.cta')}</button>
    </main>
  );
}
```

### 2. 客户端组件

```typescript
// components/LanguageSwitcher.tsx
'use client';

import { useLocale } from 'next-intl';
import { useRouter, usePathname } from 'next/navigation';

export function LanguageSwitcher() {
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();

  const switchLanguage = (newLocale: string) => {
    // 获取当前路径（不含语言前缀）
    const pathWithoutLocale = pathname.replace(`/${locale}`, '');

    // 构建新路径
    const newPath = newLocale === 'en'
      ? pathWithoutLocale || '/'
      : `/${newLocale}${pathWithoutLocale}`;

    router.push(newPath);
  };

  return (
    <select value={locale} onChange={(e) => switchLanguage(e.target.value)}>
      <option value="en">English</option>
      <option value="zh">简体中文</option>
      <option value="ja">日本語</option>
      <option value="fr">Français</option>
      {/* ... 其他语言 */}
    </select>
  );
}
```

### 3. 链接导航

```typescript
// components/Navigation.tsx
import { Link } from 'next-intl';
import { useTranslations } from 'next-intl';

export function Navigation() {
  const t = useTranslations('common.nav');

  return (
    <nav>
      {/* ✅ 正确：使用 next-intl 的 Link */}
      {/* 英文下自动生成 /about */}
      {/* 中文下自动生成 /zh/about */}
      <Link href="/">{t('home')}</Link>
      <Link href="/tests">{t('tests')}</Link>
      <Link href="/docs">{t('docs')}</Link>
      <Link href="/api">{t('api')}</Link>
      <Link href="/blog">{t('blog')}</Link>

      {/* ❌ 错误：不要使用 next/link */}
      {/* <Link href="/about">About</Link> */}
    </nav>
  );
}
```

### 4. 动态翻译（带参数）

```typescript
// locales/en.json
{
  "user": {
    "greeting": "Hello, {name}!",
    "itemsCount": "You have {count, plural, =0 {no items} =1 {one item} other {# items}}"
  }
}

// 组件中使用
const t = useTranslations('user');

<p>{t('greeting', { name: 'Alice' })}</p>
// 输出: Hello, Alice!

<p>{t('itemsCount', { count: 5 })}</p>
// 输出: You have 5 items
```

---

## 🚀 新增语言流程

### 步骤 1: 创建翻译文件

```bash
# 复制英文文件作为模板
cp locales/en.json locales/fr.json
```

### 步骤 2: 翻译内容

编辑 `locales/fr.json`，将所有英文翻译为法文：

```json
{
  "common": {
    "nav": {
      "home": "Accueil",
      "tests": "Tests",
      "docs": "Documentation",
      "api": "API",
      "blog": "Blog"
    }
  }
}
```

### 步骤 3: 注册语言

在 `middleware.ts` 和 `i18n.ts` 的 `locales` 数组中添加 `'fr'`：

```typescript
// middleware.ts
const locales = ['en', 'zh', 'ja', 'fr']; // 添加 'fr'

// i18n.ts
const locales = ['en', 'zh', 'ja', 'fr']; // 同步添加
```

### 步骤 4: 更新语言切换器

```typescript
// components/LanguageSwitcher.tsx
<option value="fr">Français</option>
```

### 步骤 5: 添加 SEO 元标签

```typescript
// app/[locale]/layout.tsx
<link rel="alternate" hrefLang="fr" href="https://browserleaks.io/fr" />
```

**完成！** 访问 `/fr` 即可看到法文版本。

---

## 📊 支持语言列表

### Phase 1: 核心语言（优先级1）

| 语言 | 代码 | URL示例 | 状态 |
|-----|------|---------|------|
| 英文 | `en` | `/about` | ✅ 默认 |
| 简体中文 | `zh` | `/zh/about` | 🔄 进行中 |
| 日文 | `ja` | `/ja/about` | 📅 计划中 |
| 韩文 | `ko` | `/ko/about` | 📅 计划中 |
| 法文 | `fr` | `/fr/about` | 📅 计划中 |
| 德文 | `de` | `/de/about` | 📅 计划中 |

### Phase 2: 扩展语言（优先级2）

| 语言 | 代码 | 市场 |
|-----|------|------|
| 西班牙文 | `es` | 西班牙、拉美 |
| 葡萄牙文 | `pt` | 巴西、葡萄牙 |
| 俄文 | `ru` | 俄罗斯、东欧 |
| 阿拉伯文 | `ar` | 中东、北非 |
| 印地文 | `hi` | 印度 |

### Phase 3: 小众语言（优先级3）

| 语言 | 代码 | 市场 |
|-----|------|------|
| 土耳其文 | `tr` | 土耳其 |
| 越南文 | `vi` | 越南 |
| 泰文 | `th` | 泰国 |
| 印尼文 | `id` | 印尼 |
| 意大利文 | `it` | 意大利 |
| 荷兰文 | `nl` | 荷兰 |
| 波兰文 | `pl` | 波兰 |
| 瑞典文 | `sv` | 瑞典 |

**目标**: 支持 20+ 语言，覆盖全球 90% 用户

---

## ✅ SEO 优化

### 1. 自动生成 hreflang 标签

```typescript
// app/[locale]/layout.tsx
const languages = [
  { code: 'en', name: 'English' },
  { code: 'zh', name: '简体中文' },
  { code: 'ja', name: '日本語' },
  // ... 其他语言
];

export default function LocaleLayout({ params }: { params: { locale: string } }) {
  return (
    <html lang={params.locale}>
      <head>
        {/* SEO: 多语言标签 */}
        {languages.map(({ code }) => (
          <link
            key={code}
            rel="alternate"
            hrefLang={code}
            href={`https://browserleaks.io${code === 'en' ? '' : `/${code}`}`}
          />
        ))}
        <link rel="alternate" hrefLang="x-default" href="https://browserleaks.io" />
      </head>
      {/* ... */}
    </html>
  );
}
```

### 2. 动态 Sitemap

```typescript
// app/sitemap.ts
import { MetadataRoute } from 'next';

const locales = ['en', 'zh', 'ja', 'ko', 'fr', 'de'];
const pages = ['/', '/tests', '/docs', '/api', '/blog'];

export default function sitemap(): MetadataRoute.Sitemap {
  const entries = [];

  for (const locale of locales) {
    for (const page of pages) {
      entries.push({
        url: `https://browserleaks.io${locale === 'en' ? '' : `/${locale}`}${page}`,
        lastModified: new Date(),
        changeFrequency: 'weekly',
        priority: page === '/' ? 1.0 : 0.8,
      });
    }
  }

  return entries;
}
```

### 3. robots.txt

```
# public/robots.txt
User-agent: *
Allow: /
Disallow: /api/

Sitemap: https://browserleaks.io/sitemap.xml
```

---

## 🚫 避坑指南

### ❌ 错误做法

```typescript
// ❌ 1. 禁止在 app/ 根目录创建 page.tsx
app/
├── page.tsx  // ❌ 错误！会导致路由冲突
└── [locale]/
    └── page.tsx

// ❌ 2. 禁止手动拼接 URL
const url = `/${locale}${path}`; // ❌ 错误！

// ❌ 3. 禁止硬编码文本
<h1>Welcome to BrowserLeaks</h1> // ❌ 错误！

// ❌ 4. 禁止使用 next/link
import Link from 'next/link'; // ❌ 错误！
```

### ✅ 正确做法

```typescript
// ✅ 1. 所有页面都在 [locale] 内
app/
└── [locale]/
    ├── page.tsx  // ✅ 正确
    └── tests/
        └── page.tsx

// ✅ 2. 使用 next-intl 的 Link
import { Link } from 'next-intl';
<Link href="/about">About</Link>

// ✅ 3. 使用翻译函数
const t = useTranslations('common');
<h1>{t('welcome')}</h1>

// ✅ 4. 使用 usePathname 获取路径
import { usePathname } from 'next/navigation';
const pathname = usePathname();
```

---

## 📈 性能优化

### 1. 翻译文件代码分割

每个语言的翻译文件独立加载，不会影响首屏性能：

```typescript
// i18n.ts 中动态加载
messages: (await import(`./locales/${locale}.json`)).default
```

### 2. 静态生成所有语言版本

```typescript
// app/[locale]/layout.tsx
export function generateStaticParams() {
  return locales.map((locale) => ({ locale }));
}
```

### 3. 服务端组件优先

大部分翻译在服务端完成，减少客户端JS体积：

```typescript
// ✅ 服务端组件（默认）
const t = useTranslations('home');

// ⚠️ 客户端组件（仅在需要交互时）
'use client';
const t = useTranslations('home');
```

---

## 🧪 测试

### 测试清单

```
☑ 默认语言 URL 无前缀 (e.g., /about)
☑ 其他语言 URL 有前缀 (e.g., /zh/about)
☑ Link 组件自动适配当前语言
☑ 浏览器语言自动检测并重定向
☑ 语言切换保持当前页面
☑ SEO 标签 <link rel="alternate" hreflang="..." /> 正确生成
☑ Sitemap 包含所有语言版本
☑ 所有文本使用翻译函数
☑ 不存在硬编码文本
☑ RTL 语言（阿拉伯文）正确显示
```

### 单元测试示例

```typescript
// __tests__/i18n.test.ts
import { getTranslations } from 'next-intl/server';

describe('Internationalization', () => {
  it('should load English translations', async () => {
    const t = await getTranslations({ locale: 'en', namespace: 'home' });
    expect(t('hero.title')).toBe('Comprehensive Browser Privacy Testing');
  });

  it('should load Chinese translations', async () => {
    const t = await getTranslations({ locale: 'zh', namespace: 'home' });
    expect(t('hero.title')).toBe('全面的浏览器隐私检测');
  });
});
```

---

## 📞 翻译协作

### 翻译流程

1. **基准翻译** (机器翻译)
   - 使用 ChatGPT/DeepL 生成初始翻译
   - 快速覆盖所有语言

2. **社区贡献** (人工校对)
   - 接受 GitHub PR
   - 母语者校对和优化

3. **持续维护**
   - 新功能添加翻译
   - 定期更新过时翻译

### 翻译质量标准

| 级别 | 标准 | 状态 |
|-----|------|------|
| L1 | 机器翻译，基本可用 | 所有语言 |
| L2 | 人工校对，准确流畅 | 核心语言 |
| L3 | 母语者审核，专业术语 | 英文、中文 |

---

## 📚 相关文档

- [next-intl 官方文档](https://next-intl-docs.vercel.app/)
- [Next.js i18n 路由](https://nextjs.org/docs/app/building-your-application/routing/internationalization)
- [ARCHITECTURE.md](./ARCHITECTURE.md) - 技术架构
- [PRD.md](./PRD.md) - 产品需求

---

**文档版本**: 1.0
**最后更新**: 2025-11-15
**维护者**: BrowserLeaks Team
